import PagePlaceholder from './PagePlaceholder';

const BranchesPage = () => (
  <PagePlaceholder
    title="Branches — coming next"
    subtitle="Per-location revenue vs. target, expense breakdown, employee headcount."
  />
);

export default BranchesPage;
