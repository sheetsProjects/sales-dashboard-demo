import PagePlaceholder from './PagePlaceholder';

const CategoryPage = () => (
  <PagePlaceholder
    title="AI Insight — coming next"
    subtitle="Deterministic category breakdown for the selected insight."
  />
);

export default CategoryPage;
