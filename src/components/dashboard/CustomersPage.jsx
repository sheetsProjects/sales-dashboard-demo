import PagePlaceholder from './PagePlaceholder';

const CustomersPage = () => (
  <PagePlaceholder
    title="Customers — coming next"
    subtitle="Acquisition channels, LTV distribution and demographic breakdowns."
  />
);

export default CustomersPage;
